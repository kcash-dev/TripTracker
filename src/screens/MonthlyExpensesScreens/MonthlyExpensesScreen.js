import React from 'react';
import { 
  View, 
  Button, 
  StyleSheet, 
  Text 
} from 'react-native';

const MonthlyExpensesScreen = ({ navigation }) => {
    return (
      <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
        <Text>This is the Monthly Expenses Screen</Text>
      </View>
    );
}

export default MonthlyExpensesScreen;